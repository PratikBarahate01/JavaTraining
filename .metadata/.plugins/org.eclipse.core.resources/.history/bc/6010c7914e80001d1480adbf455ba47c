package mapVsflatmap;

import java.util.stream.Collectors;

public class PersonService {
	public static void main(String[] args) {
		PersonDao.getAll().stream()
			.map(person->person.getEmail()).collect(Collectors.toList());
	}
}
